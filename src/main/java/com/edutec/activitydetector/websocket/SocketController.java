package com.edutec.activitydetector.websocket;

import com.edutec.activitydetector.model.CountSumTimeAverageDto;

import org.springframework.messaging.handler.annotation.MessageMapping;
import org.springframework.messaging.handler.annotation.SendTo;
import org.springframework.stereotype.Controller;

@Controller
public class SocketController {

    //@MessageMapping("/hello")
    //@SendTo("/topic/greetings")
    //public CountSumTimeAverageDto greeting(String string) throws Exception {
        //Thread.sleep(1000); // simulated delay
        //return CountSumTimeAverageDto.builder()
                            //.count(0L)
                            //.timeSumSec("0")
                            //.countPerSecond("0")
                            //.build();
    //}

}
